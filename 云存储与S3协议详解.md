# 云存储核心概念：存储桶、S3 协议与文件上传全解析

> 本文系统梳理对象存储的核心概念，包括存储桶（Bucket）、S3 协议的工作原理、存储桶的数据处理能力，以及生产环境中常用的几种文件上传方式。

---

## 目录

- [一、什么是对象存储？](#一什么是对象存储)
- [二、存储桶（Bucket）详解](#二存储桶bucket详解)
- [三、S3 协议（Amazon S3 API）](#三s3 协议amazon-s3-api)
- [四、文件的五种主要上传方式](#四文件的五种主要上传方式)
- [五、五种上传方式对比](#五五种上传方式对比)
- [六、生产环境推荐架构](#六生产环境推荐架构)
- [参考资料](#参考资料)

---

## 一、什么是对象存储？

传统文件系统使用**目录树结构**（文件夹套文件夹）来组织数据，而对象存储采用**扁平化结构**——所有的文件（称为"对象 Object"）都放在"存储桶（Bucket）"里，通过唯一的 Key 来定位。

```text
传统文件系统：                    对象存储：
📁/                              🪣 my-bucket/
  📁 images/                       ├── images/avatar/user123.jpg      (Object)
    📁 avatar/                      ├── images/2024/photo1.png         (Object)
      📄 user123.jpg                ├── videos/lesson1.mp4             (Object)
    📁 2024/                        └── documents/report.pdf           (Object)
      📄 photo1.png
  📁 videos/
    📄 lesson1.mp4
```

对象存储的三个核心概念：

| 概念 | 说明 |
|------|------|
| **Bucket（存储桶）** | 对象的容器，类似"顶级文件夹"，全局唯一命名 |
| **Object（对象）** | 存储的文件本身，包含数据 + 元数据（Meta） |
| **Key（键）** | 对象的唯一标识，类似文件路径，如 `images/avatar/user123.jpg` |

---

## 二、存储桶（Bucket）详解

存储桶是对象存储中最基本的组织单元，所有操作都围绕桶展开。

### 2.1 核心特性

- **全局唯一命名**：桶名在整个系统中必须唯一（类似域名注册，先到先得）
- **扁平命名空间**：桶内没有真正的"文件夹"，通过 Key 的 `/` 分隔符模拟目录层级
- **无限容量**：单个桶可存储任意数量的对象
- **区域绑定**：创建时需指定区域（Region），如 `ap-southeast-1`、`us-east-1`
- **两层权限控制**：桶级别策略 + 对象级别 ACL

### 2.2 桶的典型配置

```json
{
  "name": "my-project-assets",
  "region": "ap-southeast-1",
  "acl": "private",
  "cors": [
    {
      "allowedOrigins": ["https://myproject.com"],
      "allowedMethods": ["GET", "PUT", "POST", "DELETE"],
      "allowedHeaders": ["*"],
      "maxAgeSeconds": 3600
    }
  ],
  "lifecycleRules": [
    { "prefix": "temp/", "expiration": { "days": 7 } }
  ]
}
```

> **CORS 配置非常重要**：浏览器端直传文件时，S3 必须配置允许你的域名跨域访问，否则上传会被浏览器拦截。

### 2.3 图片处理能力

主流第三方云存储服务（阿里云 OSS、腾讯云 COS、七牛云、又拍云等）不只是"存文件的服务器"，还在存储桶之上提供了**实时数据处理能力**。其中最常用的就是**图片处理**——只需存一张原图，通过 URL 参数动态实现缩放、裁剪、压缩、加水印等操作，**零服务端代码**。

**核心原理**：通过 URL 参数告诉存储服务你要怎么处理图片，服务端实时处理后返回结果。

```text
原图 URL：
https://bucket.oss-cn-hangzhou.aliyuncs.com/images/photo.jpg

加参数 → 实时处理 → 返回处理后的图片
```

**各厂商图片处理能力对比**：

| 能力 | 阿里云 OSS | 腾讯云 COS | 七牛云 | 又拍云 |
|------|-----------|-----------|--------|--------|
| 缩放/裁剪 | 支持 | 支持 | 支持 | 支持 |
| 格式转换（webp/avif） | 支持 | 支持 | 支持 | 支持 |
| 质量压缩 | 支持 | 支持 | 支持 | 支持 |
| 文字水印 | 支持 | 支持 | 支持 | 支持 |
| 图片水印 | 支持 | 支持 | 支持 | 支持 |
| 缩略图 | 支持 | 支持 | 支持 | 支持 |
| 人脸识别裁剪 | 支持 | 支持 | 支持 | — |

#### URL 参数处理示例

**缩放**：

```text
# 阿里云 OSS — 缩放到宽 200px，等比缩放
https://bucket.oss-cn-hangzhou.aliyuncs.com/photo.jpg?x-oss-process=image/resize,w_200

# 腾讯云 COS — 缩放到宽 200px
https://bucket.cos.ap-guangzhou.myqcloud.com/photo.jpg?imageMogr2/thumbnail/200x
```

**裁剪**：

```text
# 从原图中心裁剪 300x300 的正方形
https://bucket.oss-cn-hangzhou.aliyuncs.com/photo.jpg?x-oss-process=image/crop,w_300,h_300,g_center
```

**加水印**：

```text
# 文字水印，位置右下角
https://bucket.oss-cn-hangzhou.aliyuncs.com/photo.jpg?x-oss-process=image/watermark,text_SGVsbG8,type_d3F5Lmp5Zw,size_30,shadow_50,t_100,g_se

# 图片水印（用存储桶里另一张图片做水印）
https://bucket.oss-cn-hangzhou.aliyuncs.com/photo.jpg?x-oss-process=image/watermark,image_cGljL3dhdGVybWFyay5wbmc,type_d3F5Lmp5Zw,size_30,t_50,g_se
```

**格式转换 + 压缩**：

```text
# 转为 webp 格式，质量 80
https://bucket.oss-cn-hangzhou.aliyuncs.com/photo.jpg?x-oss-process=image/format,webp/quality,q_80
```

**组合操作**（缩放 + 水印 + 转格式）：

```text
# 阿里云：先缩放，再加水印，最后转 webp
https://bucket.oss-cn-hangzhou.aliyuncs.com/photo.jpg?x-oss-process=image/resize,w_800/watermark,text_V0NBfg,type_d3F5Lmp5Zw,size_30,g_se/format,webp
```

#### 前端封装使用

前端只需拼接 URL 即可，通常会封装为工具函数：

```javascript
// utils/image.ts
const OSS_IMG_HOST = "https://your-bucket.oss-cn-hangzhou.aliyuncs.com"

export function ossImage(key, options) {
  let url = `${OSS_IMG_HOST}/${key}`
  const params = []

  if (options?.width || options?.height) {
    if (options.width && options.height) {
      params.push(`image/resize,m_fill,w_${options.width},h_${options.height}`)
    } else if (options.width) {
      params.push(`image/resize,w_${options.width}`)
    } else {
      params.push(`image/resize,h_${options.height}`)
    }
  }

  if (options?.format) params.push(`image/format,${options.format}`)
  if (options?.quality) params.push(`image/quality,q_${options.quality}`)

  if (params.length > 0) {
    url += `?x-oss-process=${params.join("/")}`
  }

  return url
}

// 使用：不同场景使用不同尺寸
ossImage("images/avatar.jpg", { width: 100, format: "webp" })   // 头像 100px
ossImage("images/banner.jpg", { width: 1200, format: "webp" })   // 轮播图 1200px
ossImage("images/photo.jpg", { width: 400, quality: 80 })        // 列表图 400px
```

#### 样式（Style）别名

如果某个尺寸组合经常使用，可以在存储桶控制台配置"样式"，用短别名代替长参数：

```text
# 配置样式 "thumb_200" 后
https://bucket.oss-cn-hangzhou.aliyuncs.com/photo.jpg~tpl_200x200.webp

# 等同于
https://bucket.oss-cn-hangzhou.aliyuncs.com/photo.jpg?x-oss-process=image/resize,w_200/format,webp
```

**图片处理的关键优势**：

1. **只存一张原图**，不需要手动生成多套尺寸
2. **CDN 边缘处理**，在离用户最近的节点完成处理，速度快
3. **零服务端代码**，前端自己拼接 URL 就行
4. **按需处理**，访问时才生成，不浪费存储空间

### 2.4 更多能力

除了图片处理，存储桶还提供了丰富的数据处理与管理能力，使其成为一个**文件存储 + 数据处理 + 内容分发**的一体化平台。

#### 媒体处理

| 能力 | 说明 | 典型场景 |
|------|------|---------|
| **视频转码** | 将上传的视频转为不同分辨率/格式（H.264 → H.265、MP4 → HLS/m3u8） | 短视频平台、在线教育 |
| **视频截帧** | 从视频中提取某一帧作为封面图 | 视频列表封面 |
| **视频拼接/剪辑** | 通过参数裁剪视频时间段 | 视频预览片段 |
| **音频处理** | 转码、采样率调整、音频截取 | 播客、音乐平台 |

```text
# 阿里云 OSS 视频转码示例
https://bucket.oss-cn-hangzhou.aliyuncs.com/video.mp4
  ?x-oss-process=video/transcode,c_10b/vod/resize,w_640
```

#### 文档处理

| 能力 | 说明 |
|------|------|
| **文档预览** | 将 Word/Excel/PPT/PDF 转为图片或 HTML，在线预览无需下载 |
| **格式转换** | DOC → PDF、XLS → CSV 等 |
| **网页快照** | 将 HTML 转为 PDF 或图片存档 |
| **文字识别（OCR）** | 从图片中提取文字 |

```text
# 阿里云 OSS 文档预览
https://bucket.oss-cn-hangzhou.aliyuncs.com/report.docx
  ?x-oss-process=doc/preview,c_10,p_1
```

#### 内容安全（审核）

| 能力 | 说明 |
|------|------|
| **图片鉴黄** | 自动识别涉黄/涉暴内容 |
| **文本审核** | 检测文本中的违规内容 |
| **人脸检测** | 检测图片中的人脸数量和位置 |

上传时或访问时自动触发审核，违规内容直接拦截或标记。

```text
# 七牛云 图片审核
https://bucket.qiniucdn.com/photo.jpg?aviaudit=1
```

#### 数据管理

| 能力 | 说明 |
|------|------|
| **生命周期管理** | 自动将冷数据转为低频存储/归档存储，或到期自动删除 |
| **跨区域复制** | 自动将数据同步到其他区域，做灾备 |
| **版本控制** | 保留文件的历史版本，误删可恢复 |
| **绑定自定义域名** | 用自己的域名替代 OSS 默认域名 |

#### 事件通知

存储桶可以配置**事件触发器**，当文件上传、删除等操作发生时自动通知你的服务器：

```text
用户上传文件 → S3 触发事件 → 通知你的服务器（HTTP 回调 / 消息队列）
```

**典型应用**：

- 上传图片后自动生成缩略图
- 上传视频后自动触发转码任务
- 上传完成后自动写入数据库

```json
// AWS S3 事件通知配置
{
  "LambdaFunctionConfigurations": [{
    "Events": ["s3:ObjectCreated:*"],
    "LambdaFunctionArn": "arn:aws:lambda:...",
    "Filter": {
      "Key": { "S3KeyFilter": { "FilterRules": [{ "Name": "prefix", "Value": "uploads/" }] } }
    }
  }]
}
```

#### 访问与分发

| 能力 | 说明 |
|------|------|
| **CDN 加速** | 绑定 CDN，全球就近访问，延迟更低 |
| **防盗链** | 通过 Referer 白名单/黑名单防止资源被盗用 |
| **签名 URL** | 生成带有效期的临时访问链接，防止永久泄露 |
| **Hotlink 保护** | 水印防盗——非自己域名访问时自动加水印 |

#### 存储桶能力全景

```text
┌─────────────────────────────────────────────────┐
│                  存储桶的能力                      │
├──────────────┬──────────────────────────────────┤
│  存储能力     │ 文件上传、下载、删除、版本控制       │
├──────────────┼──────────────────────────────────┤
│  图片处理     │ 缩放、裁剪、压缩、水印、格式转换    │
├──────────────┼──────────────────────────────────┤
│  媒体处理     │ 视频转码、截帧、音频处理            │
├──────────────┼──────────────────────────────────┤
│  文档处理     │ 文档预览、格式转换、OCR             │
├──────────────┼──────────────────────────────────┤
│  内容安全     │ 鉴黄、文本审核、人脸检测             │
├──────────────┼──────────────────────────────────┤
│  数据管理     │ 生命周期、跨区域复制、版本控制        │
├──────────────┼──────────────────────────────────┤
│  事件驱动     │ 上传/删除事件通知（触发后端逻辑）     │
├──────────────┼──────────────────────────────────┤
│  访问控制     │ CDN加速、防盗链、签名URL、自定义域名 │
└──────────────┴──────────────────────────────────┘
```

---

## 三、S3 协议（Amazon S3 API）

S3 不是某个具体的上传协议，而是 Amazon 提出的一套**对象存储 RESTful API 标准**。如今几乎所有主流云厂商都兼容 S3 API：

| 云厂商 | 存储服务 | S3 兼容性 |
|--------|---------|----------|
| AWS | Amazon S3 | 原生 |
| 阿里云 | OSS | 高度兼容 |
| 腾讯云 | COS | 高度兼容 |
| Cloudflare | R2 | S3 兼容 |
| 自建 | MinIO | 完全兼容 |

### 3.1 核心操作一览

S3 本质上就是一套 **HTTP API**，每个操作对应一个 HTTP 请求：

| 操作 | HTTP 方法 | 路径 | 说明 |
|------|-----------|------|------|
| 上传对象 | `PUT` | `/{bucket}/{key}` | 简单上传 |
| 下载对象 | `GET` | `/{bucket}/{key}` | 下载文件 |
| 删除对象 | `DELETE` | `/{bucket}/{key}` | 删除文件 |
| 获取元信息 | `HEAD` | `/{bucket}/{key}` | 获取大小/类型/ETag |
| 列出对象 | `GET` | `/{bucket}?list-type=2` | 列出桶内对象 |
| 初始化分片 | `POST` | `/{bucket}/{key}?uploads` | 开始分片上传 |
| 上传分片 | `PUT` | `/{bucket}/{key}?partNumber=N&uploadId=xxx` | 上传单个分片 |
| 完成分片 | `POST` | `/{bucket}/{key}?uploadId=xxx` | 合并所有分片 |

### 3.2 两种主流认证方式

#### 方式一：预签名 URL（Presigned URL）

服务器用 AccessKey 对一个具体的请求（如 PUT 到某个 key）进行签名，生成一个带签名参数的临时 URL，客户端拿到 URL 后直接向 S3 发起请求。

```text
https://bucket.s3.amazonaws.com/images/avatar.jpg
  ?X-Amz-Algorithm=AWS4-HMAC-SHA256
  &X-Amz-Credential=AKIAIOSFODNN7EXAMPLE/20240101/ap-southeast-1/s3/aws4_request
  &X-Amz-Date=20240101T120000Z
  &X-Amz-Expires=3600
  &X-Amz-Signature=xxxxxxx    ← 签名，防止伪造
```

**优点**：URL 有过期时间，泄漏后影响有限。

#### 方式二：STS 临时凭证

服务器通过 AWS STS（Security Token Service）获取一组临时凭证（AccessKeyId + SecretAccessKey + SessionToken），下发给客户端，客户端用 SDK 直接操作 S3。

**优点**：一次授权可上传多个文件，更适合分片上传场景。

```text
┌──────────┐                     ┌──────────┐                     ┌──────────┐
│  客户端    │── 1.请求上传凭证 ───▶│  后端服务  │── 2.生成签名/STS ───▶│  S3 服务  │
│ (前端)    │◀── 3.返回凭证 ──────│          │◀────────────────── │          │
│          │── 4.客户端直传 ──────────────────────────────────────▶│          │
└──────────┘                     └──────────┘                     └──────────┘
```

> **核心思想**：让文件从客户端**直接传到存储服务**，不经过你的服务器，节省带宽和服务器资源。

---

## 四、文件的五种主要上传方式

### 4.1 简单上传（Put Object）

一次 HTTP `PUT` 请求上传整个文件。

```text
客户端 ──────── PUT /{bucket}/{key} ────────▶ S3
              Body: 文件二进制数据
```

**特点**：

- 适合小文件（通常 < 5MB）
- 实现最简单，一行代码搞定
- 失败后必须整个文件重新上传

```javascript
// 前端：简单上传
async function simpleUpload(file, presignedUrl) {
  const res = await fetch(presignedUrl, {
    method: "PUT",
    body: file,
    headers: { "Content-Type": file.type },
  })
  return res.ok ? res.headers.get("ETag") : null
}
```

### 4.2 表单上传（POST with Policy）

使用 `POST` 方法，以 `multipart/form-data` 格式提交，携带 Policy 文档和签名。

**特点**：

- 适合 HTML 表单直接提交到 S3
- 使用 POST 而非 PUT
- 浏览器原生支持，无需 JavaScript

```html
<form action="https://bucket.s3.amazonaws.com/" method="POST" enctype="multipart/form-data">
  <input type="hidden" name="key" value="uploads/${filename}">
  <input type="hidden" name="AWSAccessKeyId" value="AKIAIOSFODNN7EXAMPLE">
  <input type="hidden" name="policy" value="BASE64_ENCODED_POLICY">
  <input type="hidden" name="signature" value="COMPUTED_SIGNATURE">
  <input type="file" name="file">
  <button type="submit">上传</button>
</form>
```

### 4.3 分片上传（Multipart Upload）— 大文件首选

将文件切分为多个分片（Part），每个分片独立上传，最后合并。

```text
第1步：POST /{bucket}/{key}?uploads           → 返回 uploadId
第2步：PUT /{bucket}/{key}?partNumber=1&uploadId=xxx  → 分片1
       PUT /{bucket}/{key}?partNumber=2&uploadId=xxx  → 分片2
       PUT /{bucket}/{key}?partNumber=3&uploadId=xxx  → 分片3
第3步：POST /{bucket}/{key}?uploadId=xxx       → 合并所有分片
```

**特点**：

- 适合大文件（通常 > 5MB），最大支持 5TB
- 支持并行上传，速度更快
- 支持断点续传（失败的分片单独重传）
- AWS S3 建议超过 100MB 的文件使用此方式

```javascript
// 分片上传核心逻辑
async function multipartUpload(file, bucket, key) {
  const PART_SIZE = 5 * 1024 * 1024 // 每片 5MB
  const totalParts = Math.ceil(file.size / PART_SIZE)

  // 第1步：初始化
  const { uploadId } = await s3Client.send(
    new CreateMultipartUploadCommand({ Bucket: bucket, Key: key })
  )

  const parts = []
  for (let i = 0; i < totalParts; i++) {
    const start = i * PART_SIZE
    const end = Math.min(start + PART_SIZE, file.size)
    const blob = file.slice(start, end)

    // 第2步：上传分片（可并行）
    const { ETag } = await s3Client.send(
      new UploadPartCommand({
        Bucket: bucket, Key: key,
        PartNumber: i + 1, UploadId: uploadId, Body: blob,
      })
    )
    parts.push({ PartNumber: i + 1, ETag })
  }

  // 第3步：合并
  await s3Client.send(
    new CompleteMultipartUploadCommand({
      Bucket: bucket, Key: key, UploadId: uploadId,
      MultipartUpload: { Parts: parts },
    })
  )
}
```

### 4.4 预签名 URL 直传 — 前后端分离最常用

服务器生成一个带签名的临时 URL，前端拿到后直接上传到 S3，**文件不经过后端服务器**。

```text
┌──────────┐    1.我要上传 file.jpg    ┌──────────┐
│  前端     │ ────────────────────────▶│  后端 API  │
│          │                          │          │
│          │◀──── 2.返回 Presigned URL │          │
│          │                          └──────────┘
│          │    3.PUT file.jpg 直传        │
│          │ ──────────────────────────────────────▶ S3
│          │◀──── 4.200 OK (ETag) ──────────────────  │
└──────────┘
```

```javascript
// 服务端（Node.js）生成预签名 URL
import { S3Client, PutObjectCommand } from "@aws-sdk/client-s3"
import { getSignedUrl } from "@aws-sdk/s3-request-presigner"

const s3Client = new S3Client({
  region: "ap-southeast-1",
  credentials: { accessKeyId: "xxx", secretAccessKey: "xxx" },
})

async function getUploadUrl(key, contentType) {
  const command = new PutObjectCommand({
    Bucket: "my-bucket",
    Key: key,
    ContentType: contentType,
  })
  return getSignedUrl(s3Client, command, { expiresIn: 3600 })
}
```

### 4.5 STS 临时凭证直传 — 小程序/移动端推荐

服务器通过 STS 获取临时凭证下发给客户端，客户端用 SDK 直接上传。

```javascript
// 服务端：获取 STS 临时凭证
const sts = new STSClient({ region: "ap-southeast-1" })
const { Credentials } = await sts.send(new GetFederationTokenCommand({
  Name: "user-upload",
  Policy: JSON.stringify({
    Version: "2012-10-17",
    Statement: [{
      Effect: "Allow",
      Action: ["s3:PutObject"],
      Resource: [`arn:aws:s3:::my-bucket/uploads/${userId}/*`],
    }],
  }),
  DurationSeconds: 3600,
}))

// 返回给客户端
return {
  accessKeyId: Credentials.AccessKeyId,
  secretAccessKey: Credentials.SecretAccessKey,
  sessionToken: Credentials.SessionToken,
  bucket: "my-bucket",
  region: "ap-southeast-1",
}
```

```javascript
// 客户端（小程序）：用临时凭证直传
const s3 = new S3Client({
  region: res.region,
  credentials: {
    accessKeyId: res.accessKeyId,
    secretAccessKey: res.secretAccessKey,
    sessionToken: res.sessionToken,
  },
})

await s3.send(new PutObjectCommand({
  Bucket: res.bucket,
  Key: `uploads/${userId}/${Date.now()}.jpg`,
  Body: fileBuffer,
  ContentType: "image/jpeg",
}))
```

---

## 五、五种上传方式对比

| 上传方式 | 适用文件大小 | 是否经过服务器 | 实现复杂度 | 断点续传 | 典型场景 |
|----------|------------|--------------|----------|---------|---------|
| 简单上传 | < 5MB | 可选 | 最低 | 否 | 头像、小图片 |
| 表单上传 | < 5MB | 否 | 低 | 否 | 传统网页表单 |
| 分片上传 | > 5MB | 可选 | 高 | 是 | 视频大文件 |
| 预签名 URL | 任意 | 否 | 中 | 需自实现 | Web 前端直传 |
| STS 直传 | 任意 | 否 | 高 | 原生支持 | 小程序/移动端 |

---

## 六、生产环境推荐架构

```text
                        ┌─────────────────────────────────────┐
                        │            S3 / OSS / R2             │
                        │          （对象存储服务）              │
                        └──────────┬──────────────┬───────────┘
                                   │              │
                            直传文件流       返回元数据
                                   │              │
┌──────────┐    1.请求上传凭证     ┌──────────┐    │
│  客户端    │ ◀───────────────────│  后端 API  │◀───┘
│ (前端/小程序)│ ─── 3.返回凭证 ───▶│          │
│          │ ─── 4.直传文件 ──────────────────▶│ (S3)
│          │ ◀──── 5.上传成功 ────────────────│
└──────────┘                      ┌──────────┐
                                 │  数据库    │
  6.回调通知 ─────────────────────▶│          │
                                 │ file_key │
                                 │ file_url │
                                 │ file_size│
                                 └──────────┘
```

**关键设计要点**：

1. **文件直传到存储服务**，后端只负责签发凭证和记录元数据
2. **CORS 必须配置**，否则浏览器会拦截跨域上传请求
3. **上传回调**：上传成功后，前端通知后端记录文件信息（key、size、type），后端也可以通过 S3 的事件通知自动触发
4. **权限最小化**：STS 凭证应限制到具体的路径前缀和操作类型

---

## 参考资料

- [Amazon S3 官方文档](https://docs.aws.amazon.com/s3/)
- [AWS SDK for JavaScript v3](https://docs.aws.amazon.com/AWSJavaScriptSDK/v3/latest/)
- [阿里云 OSS S3 兼容文档](https://help.aliyun.com/document_detail/32070.html)
